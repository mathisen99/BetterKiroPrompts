{
  "name": "secret-scan",
  "description": "Scan for secrets",
  "version": "1.0.0",
  "enabled": true,
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "askAgent",
    "prompt": "Check for accidentally committed secrets or API keys"
  }
}

{
  "name": "run-tests",
  "description": "Run unit tests",
  "version": "1.0.0",
  "enabled": true,
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "Run the test suite and summarize results"
  }
}

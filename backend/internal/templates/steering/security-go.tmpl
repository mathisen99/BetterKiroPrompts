---
inclusion: fileMatch
fileMatchPattern: "**/*.go"
---

# Go Security

## Rules

- No secrets in code — use environment variables
- Validate all input before processing
- Auth boundaries must be explicit in handlers
- Use least privilege for database queries
- Wrap errors without exposing internals

# =============================================================================
# BetterKiroPrompts Environment Variables
# =============================================================================
#
# This file contains SECRETS and sensitive configuration values.
# Copy this file to `.env` and fill in your actual values.
#
# IMPORTANT: Configuration Split
# ------------------------------
# - SECRETS (API keys, tokens, passwords) → .env (this file)
# - SETTINGS (ports, timeouts, limits)   → config.toml
#
# See config.example.toml for all application settings.
#
# Precedence (highest to lowest):
#   1. Environment variables (.env)
#   2. config.toml values
#   3. Built-in defaults
#
# =============================================================================

# =============================================================================
# REQUIRED SECRETS
# =============================================================================
# These values MUST be set for the application to function.

# OpenAI API Key (REQUIRED)
# Get your key at: https://platform.openai.com/api-keys
# This is the only required secret for basic functionality
OPENAI_API_KEY=sk-your-api-key-here

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection settings.
# In Docker Compose, these are used to initialize the database container.

# Database connection URL for the backend
# Format: postgres://user:password@host:port/database?sslmode=disable
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable

# PostgreSQL container initialization (used by docker-compose)
POSTGRES_USER=bkp_user
POSTGRES_PASSWORD=CHANGE_ME_IN_PRODUCTION
POSTGRES_DB=betterkiro

# =============================================================================
# OPTIONAL SECRETS
# =============================================================================
# These enable additional features but are not required.

# GitHub Personal Access Token (optional)
# Required ONLY for scanning private repositories
# Generate at: https://github.com/settings/tokens
# Required scopes: repo (for private repos)
# Leave empty for public-only scanning
GITHUB_TOKEN=

# =============================================================================
# ENVIRONMENT VARIABLE OVERRIDES
# =============================================================================
# These override config.toml values. Use sparingly - prefer config.toml for
# non-secret settings. These are provided for backward compatibility and
# container orchestration scenarios.
#
# Server
# ------
# PORT=8090                              # Override server.port
#
# Logging
# -------
# LOG_LEVEL=INFO                         # Override logging.level (DEBUG|INFO|WARN|ERROR)
# NO_COLOR=1                             # Disable colored console output
#
# OpenAI
# ------
# OPENAI_MODEL=gpt-5.2                   # Override openai.model
#
# Rate Limiting
# -------------
# RATE_LIMIT_GENERATION=10               # Override rate_limit.generation_limit_per_hour
# RATE_LIMIT_RATING=20                   # Override rate_limit.rating_limit_per_hour
# RATE_LIMIT_SCAN=10                     # Override rate_limit.scan_limit_per_hour
#
# Scanner
# -------
# SCANNER_MAX_REPO_SIZE_MB=500           # Override scanner.max_repo_size_mb
# SCANNER_MAX_REVIEW_FILES=10            # Override scanner.max_review_files
# SCANNER_TOOL_TIMEOUT_SECONDS=300       # Override scanner.tool_timeout_seconds
# SCANNER_RESULT_RETENTION_DAYS=7        # Override scanner.retention_days

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Vite environment variables for the frontend build.

# API URL for frontend to connect to backend
# In development: http://localhost:8090
# In production: your deployed backend URL
VITE_API_URL=http://localhost:8090
